# Used by kafka-debezium/generate_and_post_connector.py
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=banking

# Optional (kept for compatibility with existing local setups)
POSTGRES_HOST=postgres
